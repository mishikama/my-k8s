envs:
  RAILS_DEVELOPMENT_HOSTS: "ruby-51-250-109-184.nip.io"

deployments:
  web-app:
    replicas: 2

ingresses:
  ruby-51-250-109-184.nip.io:
    certManager:
      issuerType: ClusterIssuer
      issuerName: letsencrypt-dev
    ingressClassName: nginx
    hosts:
      - paths:
          - serviceName: web-app
            servicePort: 3000
            path: "/"
